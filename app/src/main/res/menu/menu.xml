<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:icon="@drawable/ic_menu"
        android:title=""
        app:showAsAction="ifRoom">
        <menu>

            <item
                android:id="@+id/menu_privacy_policy"
                android:title="@string/p_pol"
                android:contentDescription="@string/p_pol"
                app:showAsAction="never" />

            <item
                android:id="@+id/menu_password"
                android:contentDescription="@string/passcode_lock"
                android:title="@string/passcode_lock"
                app:showAsAction="never" />
        </menu>
    </item>
</menu>